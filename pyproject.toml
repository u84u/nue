[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "nue"
version = "0.1.0"
description = "Nue — Native 1-Bit Instruction Model Research"
authors = [
    {name = "User"}
]
dependencies = [
    "torch",
    "numpy",
    "tqdm",
    "matplotlib",
    "tornado",
]
requires-python = ">=3.10"
