[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "native1bit"
version = "0.1.0"
description = "Native 1-Bit Instruction Model Research"
authors = [
    {name = "User"}
]
dependencies = [
    "torch",
    "numpy",
    "tqdm",
]
requires-python = ">=3.10"
